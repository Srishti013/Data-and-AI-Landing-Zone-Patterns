<policies>

  <inbound>
    <base />

    <rewrite-uri 
        template="${rewrite_template}" 
        copy-unmatched-params="${rewrite_copy_unmatched_params}" />

  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
  </outbound>

  <on-error>
    <base />
  </on-error>

</policies>