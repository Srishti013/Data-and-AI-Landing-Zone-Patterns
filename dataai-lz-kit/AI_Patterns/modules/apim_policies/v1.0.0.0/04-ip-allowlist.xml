<policies>

  <inbound>
    <base />

    <ip-filter action="${ip_filter_action}">
      ${ip_ranges_block}
    </ip-filter>

  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
  </outbound>

  <on-error>
    <base />
  </on-error>

</policies>