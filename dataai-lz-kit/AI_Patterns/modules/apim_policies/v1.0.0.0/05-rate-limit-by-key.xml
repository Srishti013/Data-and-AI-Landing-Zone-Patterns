<policies>

  <inbound>
    <base />

    <rate-limit-by-key
        calls="${rate_limit_calls}"
        renewal-period="${rate_limit_period}"
        counter-key="${rate_limit_counter_key}">

        
    </rate-limit-by-key>

  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
  </outbound>

  <on-error>
    <base />
  </on-error>

</policies>