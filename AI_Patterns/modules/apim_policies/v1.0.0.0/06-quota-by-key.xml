<policies>

  <inbound>
    <base />

    <quota-by-key
        calls="${quota_calls}"
        renewal-period="${quota_period}"
        counter-key="${quota_counter_key}" />

  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
  </outbound>

  <on-error>
    <base />
  </on-error>

</policies>