{
  "displayName": "Azure OpenAI Service: Encrypt data with a customer-managed key",
  "description": "Denies Microsoft.CognitiveServices OpenAI accounts whose encryption.keySource is not Microsoft.KeyVault, ensuring data at rest is protected by a customer-managed key. MCSB v2 DP-5.1.",
  "mode": "All",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.CognitiveServices/accounts" },
        { "field": "kind", "equals": "OpenAI" },
        { "field": "Microsoft.CognitiveServices/accounts/encryption.keySource", "notEquals": "Microsoft.KeyVault" }
      ]
    },
    "then": { "effect": "[parameters('effect')]" }
  },
  "parameters": {
    "effect": {
      "type": "String",
      "metadata": { "displayName": "Effect", "description": "Enable or disable the execution of the policy." },
      "allowedValues": ["Audit", "Deny", "Disabled"],
      "defaultValue": "Deny"
    }
  }
}
