{
  "displayName": "Azure API Management: Disable public network access",
  "description": "Audits Microsoft.ApiManagement/service whose publicNetworkAccess is not Disabled. Audit-only: Internal-mode APIM cannot set publicNetworkAccess=Disabled at creation (no private endpoint) and is already private via the VNet. MCSB v2 NS-2.1, NS-2.3.",
  "mode": "All",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.ApiManagement/service" },
        { "field": "Microsoft.ApiManagement/service/publicNetworkAccess", "notEquals": "Disabled" }
      ]
    },
    "then": { "effect": "[parameters('effect')]" }
  },
  "parameters": {
    "effect": { "type": "String", "metadata": { "displayName": "Effect" }, "allowedValues": ["Audit", "Deny", "Disabled"], "defaultValue": "Audit" }
  }
}
