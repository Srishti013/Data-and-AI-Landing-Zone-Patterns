{
  "displayName": "Azure OpenAI Service: Default-deny network ACLs",
  "description": "Denies Microsoft.CognitiveServices OpenAI accounts whose networkAcls.defaultAction is not Deny, so all traffic not explicitly permitted by IP or virtual network rules is blocked. MCSB v2 NS-2.3.",
  "mode": "All",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.CognitiveServices/accounts" },
        { "field": "kind", "equals": "OpenAI" },
        { "field": "Microsoft.CognitiveServices/accounts/networkAcls.defaultAction", "notEquals": "Deny" }
      ]
    },
    "then": { "effect": "[parameters('effect')]" }
  },
  "parameters": {
    "effect": {
      "type": "String",
      "metadata": { "displayName": "Effect", "description": "Enable or disable the execution of the policy." },
      "allowedValues": ["Audit", "Deny", "Disabled"],
      "defaultValue": "Deny"
    }
  }
}
