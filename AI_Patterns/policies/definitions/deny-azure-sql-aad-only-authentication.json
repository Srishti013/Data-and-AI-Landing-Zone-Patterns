{
  "displayName": "Azure SQL: Enforce Azure AD-only authentication",
  "description": "Denies Microsoft.Sql/servers/azureADOnlyAuthentications where azureADOnlyAuthentication is not true, disabling SQL local authentication. Covers the SQL Database + SQL Server AAD-only matrix rows. MCSB v2 IM-1.1, PA-7.1.",
  "mode": "All",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.Sql/servers/azureADOnlyAuthentications" },
        { "field": "Microsoft.Sql/servers/azureADOnlyAuthentications/azureADOnlyAuthentication", "notEquals": true }
      ]
    },
    "then": { "effect": "[parameters('effect')]" }
  },
  "parameters": {
    "effect": { "type": "String", "metadata": { "displayName": "Effect" }, "allowedValues": ["Audit", "Deny", "Disabled"], "defaultValue": "Deny" }
  }
}
