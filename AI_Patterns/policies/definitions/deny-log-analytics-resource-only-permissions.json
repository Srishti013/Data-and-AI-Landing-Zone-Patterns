{
  "displayName": "Azure Log Analytics Workspace: Use resource-context access mode for log queries",
  "description": "Denies Microsoft.OperationalInsights/workspaces where features.enableLogAccessUsingOnlyResourcePermissions is not true, so users only see logs for resources they can access. MCSB v2 PA-7.1.",
  "mode": "All",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.OperationalInsights/workspaces" },
        { "field": "Microsoft.OperationalInsights/workspaces/features.enableLogAccessUsingOnlyResourcePermissions", "notEquals": true }
      ]
    },
    "then": { "effect": "[parameters('effect')]" }
  },
  "parameters": {
    "effect": { "type": "String", "metadata": { "displayName": "Effect" }, "allowedValues": ["Audit", "Deny", "Disabled"], "defaultValue": "Deny" }
  }
}
